import React from 'react';
import { useParams } from 'react-router-dom';

const DetailsReview = () => {
    const {id} = useParams()
    return (
        <div>
            
        </div>
    );
};

export default DetailsReview;